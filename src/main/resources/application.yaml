server:
  port: 8080
spring:
  profiles:
    active: dev

mybatis:
  mapper-locations: classpath:mapper/*.xml
logging:
  level:
    com.jeb.framework.mapper: debug
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

